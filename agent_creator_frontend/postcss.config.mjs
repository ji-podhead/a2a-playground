/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: [
    '@tailwindcss/postcss',
    // 'autoprefixer', // autoprefixer can also be added if needed
  ],
};
export default config;
