# URLs for the worker agent services for Docker Compose
# These should use the service names defined in docker-compose.yml
FLIGHT_AGENT_URL=http://flight_agent_service:8001
HOTEL_AGENT_URL=http://hotel_agent_service:8002

# Port for the Gradio UI (optional, if you want to configure it via .env for host_agent/app_ui.py)
# GRADIO_SERVER_PORT=8080

# Gemini API Key (if your ADK setup for the Routing Agent needs it directly)
# Ensure this is available to the host_agent_ui_service if required by its ADK configuration.
# GOOGLE_API_KEY=your_gemini_api_key_here

# ADK Configuration (Optional, ADK might use gcloud defaults or GOOGLE_APPLICATION_CREDENTIALS)
# Ensure these are available to the host_agent_ui_service if needed.
# ADK_PROJECT_ID=your_gcp_project_id
# ADK_LOCATION=your_gcp_location # e.g., us-central1
